import colors from "./colors";
import strings from "./strings";
import urls from "./urls";

export default {
  TOKEN: "",
  colors: colors,
  strings: strings,
  urls: urls,
  styles: {
    container: {
      flex: 1,
      paddingHorizontal: 10
    }
  }
};
